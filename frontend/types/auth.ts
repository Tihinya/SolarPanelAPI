export interface LoginFormSubmitPayload {
    email: string
    password: string
}

// Define the structure of the payload emitted on register
export interface RegisterFormSubmitPayload {
    email: string
    password: string
}
